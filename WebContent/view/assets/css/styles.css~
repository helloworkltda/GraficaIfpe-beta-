@CHARSET "UTF-8";

/* CSS by HelloWork Ltda.

Colocar aqui padrões visuais das páginas de "Login" e "Cadastro"

*/

/* ---------------Aspectos Visuais Gerais do Body--------------- */

* {
	-webkit-box-sizing: border-box;
	   -moz-box-sizing: border-box;
	        box-sizing: border-box;
	outline: none;
}
body {
	background: url(../images/verde7.jpg) no-repeat center center fixed;
    -webkit-background-size: cover;
    -moz-background-size: cover;
    -o-background-size: cover;
    background-size: cover;
}

/* ---------------Logo--------------- */

.logoinicio {

	z-index: 1;
	position: fixed;
	top: 22px;
	left: 50%;
    width: 500px;
    margin-left: -250px;
    text-align: center;

}

/* ---------------Form do Login--------------- */
	
.login-form {
	text-align: center;
	margin: 0 auto;
	margin-top: 40px;
	margin-bottom: 1px;
	max-width: 420px;
}
	.login-form > section {
		padding: 35px 35px 25px 35px;
		border-radius: 5px;
	}
	.login-form a {
		color: #fff;
	}
	.login-form img {
		display: block;
		margin: 0 auto;
		margin-bottom: 35px;
	}

form[role=login] {
	font: 18px/2.2em Lato, serif;
	color: #fff;
}	
	form[role=login] input,
	form[role=login] > button {
		font-size: 15px;
		font-weight: none;
		padding: 10px 30px;
	}
	form[role=login] input {
		
		background: rgba(250,250,250,0.9);
		border: 9px solid #ccc;
		height: 2.9em;
		padding: 10px 40px;
		border-radius: 7px;
		
		-webkit-box-shadow: inset 0 0 1px 1px rgba(150, 150, 150, .1);
		-moz-box-shadow: inset 0 0 1px 1px rgba(150, 150, 150, .1);
		box-shadow: inset 0 0 1px 1px rgba(150, 150, 150, .1);

	}
	form[role=login] > div {
		margin: 30px 0;
		position: relative; 
	}
	form[role=login] > button {
		margin-top: 15px;
		margin-bottom: 15px;
		font-weight: bold;
		background: #0c56a9;
		border: 2px solid #0c4584;
		border-radius: 8px;
	}
	
.form-control + .glyphicon {
    position: absolute;
    left: 0;
	top: 14%;
    padding: 8px 0 0 17px;
	font-size: 16px;
	color: #8A978C;
}
